[bdist_wheel]
python-tag=py3

[tool:pytest]
norecursedirs = .* build dist CVS _darcs *.egg venv old
addopts = --cov --cov-report xml

[versioneer]
VCS = git
style = pep440
versionfile_source = login_otp/_version.py
versionfile_build = login_otp/_version.py
tag_prefix =
parentdir_prefix = login_otp-

